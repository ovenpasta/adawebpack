# HAC sources are a separate checkout of the fork (see README), not vendored here.
hac/

# Generated by the Makefile from the HAC fork's exm/*.adb example programs.
samples.js

# Logo / favicon / social-card image, generated from hac/obj/hac_icon.ico.
hac_icon.png

# Assembled by `make deploy` (publishable site bundle).
dist/

# Build outputs (the wasm modules, emscripten JS glue, object dirs, native
# binary and test log are all generated by the Makefile).
.objs/
.objs_cli/
*.wasm
hac_web.js
hac_cli.js
/hac_web
output.lst
