project Basic is

   for Target use "llvm";
   for Object_Dir use ".objs";
   for Source_Dirs use (".", "../bindings", ".objs");
   for Main use ("sdl3_ada_main.adb");

   package Compiler is
      for Switches ("Ada") use ("--target=wasm32", "-O1", "-gnatp");
   end Compiler;

end Basic;
